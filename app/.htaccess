RewriteEngine On

# Redirect root to index.php
RewriteRule ^$ /index.php [L]

# Route API calls
# e.g., /api/auth/login -> /api/auth.php?path=/login
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . index.php [L]
