[mysqld]
# reduce buffer size
innodb_buffer_pool_size = 8M
innodb_buffer_pool_instances = 1
innodb_log_buffer_size = 1M

# fewer connections
max_connections   = 5
thread_cache_size = 2

# disable extras to save RAM
performance_schema = 0

# no replication
skip-log-bin